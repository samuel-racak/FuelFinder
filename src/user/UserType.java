package user;

/**
 * The UserType enum represents the type of a user.
 * It can be either a basic user, a premium user or an admin.
 */
public enum UserType {
    BASIC_USER,
    PREMIUM_USER,
    ADMIN
}
